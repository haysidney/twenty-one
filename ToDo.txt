- Investigate mapping RaptureShellModule->ChatType int values to the existing channel
  command strings (/p, /say, /fc, /ls1, etc.) and use ChangeChatChannel for ALL channel
  switching (narration and hit rolls), rather than sending channel switch commands via
  ProcessChatBoxEntry. This would be cleaner and avoid any unwanted sending bare
  channel commands through the chat box.

- Add a betting announcement.

- Trade help/automation.

- Player targeting.

- Better payout screen.

- Round summary

- Splits/Doubles

- Shout timer for advertising

- Add /tell as a channel?